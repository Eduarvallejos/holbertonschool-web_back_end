// Definition of the ClassRoom class
export default class ClassRoom {
    // Constructor of the ClassRoom class
    // It is initialized with an optional maximum size for the students
    constructor(maxStudentsSize = 0) {
        this._maxStudentsSize = maxStudentsSize; // Initialization of the maximum student size
    }
}
